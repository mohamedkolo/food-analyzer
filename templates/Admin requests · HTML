{% extends "base.html" %}
{% block title %}طلبات العملاء{% endblock %}
{% block content %}

<div class="page-title">
  <i class="fa-solid fa-bell"></i> طلبات العملاء
</div>

<div class="alert alert-info">
  <i class="fa-solid fa-circle-info"></i>
  <div>
    هنا هتشوف كل طلبات الخطط من العملاء. تقدر تولدلهم خطة AI أو تصممها بإيدك. بعد ما توافق، هتظهر للعميل في حسابه.
  </div>
</div>

<div style="display:grid;gap:12px">
  {% for req in requests %}
  {% set rdata = req.request_data | from_json if req.request_data else {} %}
  <div class="card" style="padding:16px">
    <div style="display:flex;justify-content:space-between;align-items:flex-start;gap:12px;flex-wrap:wrap;margin-bottom:12px">
      <div>
        <div style="font-size:17px;font-weight:800;color:var(--blue)">
          <i class="fa-solid fa-user-circle"></i> {{ req.client_name or 'عميل' }}
        </div>
        <div style="font-size:12px;color:var(--muted);margin-top:4px">
          <i class="fa-regular fa-clock"></i> {{ req.created_at }}
        </div>
      </div>
      <div>
        {% if req.status == 'pending' %}
          <span style="background:#fff3cd;color:#856404;padding:5px 12px;border-radius:100px;font-size:12px;font-weight:700">
            ⏳ جديد - بانتظار
          </span>
        {% elif req.status == 'approved' %}
          <span style="background:#dcfce7;color:#14532d;padding:5px 12px;border-radius:100px;font-size:12px;font-weight:700">
            ✓ تم التسليم
          </span>
        {% else %}
          <span style="background:#e2e8f0;color:#475569;padding:5px 12px;border-radius:100px;font-size:12px;font-weight:700">
            {{ req.status }}
          </span>
        {% endif %}
      </div>
    </div>

    <!-- Request details -->
    <div style="background:var(--gray);padding:12px;border-radius:10px;margin-bottom:12px">
      <div style="display:grid;grid-template-columns:repeat(auto-fit,minmax(120px,1fr));gap:10px;font-size:12px">
        <div><strong>الهدف:</strong> {{ {'weight_loss':'تخسيس','maintenance':'مكتنز','muscle_gain':'عضل','bulking':'تضخيم'}.get(req.status, '—') }}</div>
        <div><strong>المطبخ:</strong> مصري / خليجي / شامي</div>
        <div><strong>العمر:</strong> —</div>
        <div><strong>الوزن:</strong> —</div>
      </div>
    </div>

    {% if req.status == 'pending' %}
    <div style="display:flex;gap:8px;flex-wrap:wrap">
      <a href="/admin/requests/{{ req.id }}/generate" class="btn btn-primary" style="flex:1;min-width:150px">
        <i class="fa-solid fa-wand-magic-sparkles"></i> ولّد خطة AI
      </a>
    </div>
    {% else %}
    <div style="text-align:center;padding:10px;color:var(--muted);font-size:13px">
      <i class="fa-solid fa-check-circle" style="color:var(--green-light)"></i> تم تسليم الخطة
    </div>
    {% endif %}
  </div>
  {% else %}
  <div class="card" style="text-align:center;padding:40px">
    <i class="fa-solid fa-inbox" style="font-size:48px;color:var(--muted-2);margin-bottom:12px"></i>
    <h3 style="color:var(--muted);font-weight:700">لا توجد طلبات حالياً</h3>
    <p style="color:var(--muted-2);font-size:14px;margin-top:6px">
      لما أي عميل يطلب خطة، هتظهر هنا
    </p>
  </div>
  {% endfor %}
</div>

{% endblock %}
